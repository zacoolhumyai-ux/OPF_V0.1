
-- One Piece Final Nerd V1 | Full Script | Rayfield UI
local ok, Rayfield = pcall(function()
    return loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
end)
if not ok or not Rayfield then
    warn("Rayfield failed to load: " .. tostring(Rayfield))
    return
end

local Players = game:GetService("Players")
local player  = Players.LocalPlayer

local ModuleScript = game:GetService("ReplicatedFirst"):FindFirstChildOfClass("ModuleScript")
local v1
if ModuleScript then
    local reqOk, result = pcall(require, ModuleScript)
    if reqOk then v1 = result end
end

local function fire(p1, p2)
    if not v1 then return end
    local s, e = pcall(function() return v1["\t"](p1, p2) end)
    if not s then warn("fire error:", e) end
end

local function getData()
    if not _G.Data or not player then return nil end
    return _G.Data[player.UserId]
end

local Window = Rayfield:CreateWindow({
    Name = "One Piece Final Nerd V1",
    Icon = 0,
    LoadingTitle = "One Piece Final Nerd",
    LoadingSubtitle = "by V1",
    Theme = "Default",
    DisableRayfieldPrompts = false,
    DisableBuildWarnings = false,
    ConfigurationSaving = { Enabled=true, FolderName="OPFinalNerd", FileName="Config" },
    KeySystem = true,
    KeySettings = {
        Title = "One Piece Final Nerd",
        Subtitle = "Key Required",
        Note = "ติดต่อรับ Key ที่ Discord",
        FileName = "OPFinalNerdKey",
        SaveKey = true,
        GrabKeyFromSite = false,
        Key = { "coz12" },
    },
})
-- ══════════════════════════════
-- TELEPORT TAB
-- ══════════════════════════════
local TeleportTab = Window:CreateTab("Teleport", 4483345998)

local function tpTo(target)
    local char = player.Character or player.CharacterAdded:Wait()
    local hrp = char:WaitForChild("HumanoidRootPart")
    local root
    if typeof(target) == "Instance" then
        if target:IsA("BasePart") then
            root = target
        elseif target:IsA("Model") then
            root = target.PrimaryPart or target:FindFirstChildWhichIsA("BasePart")
        else
            root = target:FindFirstChildWhichIsA("BasePart")
        end
    end
    if root then
        hrp.CFrame = root.CFrame * CFrame.new(0, 0, 3)
        Rayfield:Notify({ Title="Teleport", Content="ไปที่ "..target.Name.." แล้ว!", Duration=2 })
    else
        Rayfield:Notify({ Title="Teleport", Content="ไม่พบ target: "..tostring(target), Duration=3 })
    end
end

local function getTarget(path)
    local ok2, result = pcall(path)
    if ok2 and result then return result end
    return nil
end

-- ── NPCs ──
TeleportTab:CreateSection("NPCs")

TeleportTab:CreateButton({
    Name = "Secret Dealer",
    Description = "Teleport ไปหา Secret Dealer",
    Callback = function()
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        local ok2, dealer = pcall(function()
            return workspace.Ignore.NPCs.Secret:FindFirstChild("Secret Dealer")
        end)
        if not ok2 or not dealer then
            Rayfield:Notify({ Title="Error", Content="ไม่พบ Secret Dealer!", Duration=3 })
            return
        end
        local root = dealer:FindFirstChild("HumanoidRootPart")
            or dealer.PrimaryPart
            or dealer:FindFirstChildWhichIsA("BasePart")
        if root then
            hrp.CFrame = root.CFrame * CFrame.new(0, 0, 3)
            Rayfield:Notify({ Title="Teleport", Content="ไปที่ Secret Dealer แล้ว!", Duration=2 })
        else
            Rayfield:Notify({ Title="Error", Content="ไม่พบ root part!", Duration=3 })
        end
    end,
})

-- ── Islands ──
TeleportTab:CreateSection("Islands")

local islandList = {
    { name = "Snowy Mountains",    fn = function() return workspace.MapFolder.IslandSnowyMountains.Stone:GetChildren()[40] end },
    { name = "Crescent Mountain",  fn = function() return workspace.MapFolder.IslandCrescent.Mountain:GetChildren()[13] end },
    { name = "Cliffs",             fn = function() return workspace.MapFolder.IslandCliffs.Grass:GetChildren()[7] end },
    { name = "Island 13",          fn = function() return workspace.MapFolder.Island13.Build.Colorables:GetChildren()[108] end },
    { name = "Island 8 House",     fn = function() return workspace.MapFolder.Island8.House2:GetChildren()[201] end },
    { name = "Tree Island",        fn = function() return workspace.MapFolder.IslandTREEA.BigHouse:GetChildren()[320] end },
    { name = "Alabata",            fn = function() return workspace.MapFolder.Alabata.Model:GetChildren()[7368] end },
    { name = "Rocky Island",       fn = function() return workspace.MapFolder.IslandRocky:GetChildren()[15] end },
    { name = "Snowy Mini Mountain", fn = function() return workspace.MapFolder.IslandSnowy.MiniMountain:GetChildren()[6] end },
    { name = "Cave Island",        fn = function() return workspace.MapFolder.IslandCaver.Stones:GetChildren()[193] end },
    { name = "Island 11 Moss",     fn = function() return workspace.MapFolder.Island11.Moss:GetChildren()[85] end },
    { name = "Z Island 222",       fn = function() return workspace.MapFolder.Z_Island222.Model:GetChildren()[200] end },
    { name = "Jail Island Beach",  fn = function() return workspace.MapFolder["Jail Islands "].Beach end },
    { name = "Evil Island",        fn = function() return workspace.MapFolder.IslandEvil.IslandEvil:GetChildren()[3] end },
    { name = "Senna Island",       fn = function() return workspace.MapFolder.IslandSenna.Objects:GetChildren()[47] end },
    { name = "Kai Island Aura",    fn = function() return workspace.MapFolder.IslandKai.AuraReceptical end },
    { name = "Island 12 Rocks",    fn = function() return workspace.MapFolder.Island12.Rocks:GetChildren()[28] end },
    { name = "Rocky Islands",      fn = function() return workspace.MapFolder["Rocky Islands"]:GetChildren()[86] end },
    { name = "Marine Ford",        fn = function() return workspace.Ignore.NPCs.Islands["Reach Marine Ford"] end },
}

for _, loc in ipairs(islandList) do
    TeleportTab:CreateButton({
        Name = loc.name,
        Description = "Teleport ไป "..loc.name,
        Callback = function()
            local target = getTarget(loc.fn)
            if target then
                tpTo(target)
            else
                Rayfield:Notify({ Title="Error", Content="ไม่พบ "..loc.name, Duration=3 })
            end
        end,
    })
end

-- ── Soul Rooms ──
TeleportTab:CreateSection("Soul Rooms")

local soulList = {
    { name = "Altar 1",   fn = function() return workspace.SoulRooms.Altar1 end },
    { name = "Altar 2",   fn = function() return workspace.SoulRooms.Altar2 end },
    { name = "Altar 3",   fn = function() return workspace.SoulRooms.Altar3 end },
    { name = "Altar 4",   fn = function() return workspace.SoulRooms.Altar4 end },
    { name = "Soul Room", fn = function() return workspace.SoulRooms.SoulRoom end },
}

for _, loc in ipairs(soulList) do
    TeleportTab:CreateButton({
        Name = loc.name,
        Description = "Teleport ไป "..loc.name,
        Callback = function()
            local target = getTarget(loc.fn)
            if target then
                tpTo(target)
            else
                Rayfield:Notify({ Title="Error", Content="ไม่พบ "..loc.name, Duration=3 })
            end
        end,
    })
end
-- ══════════════════════════════
-- AUTO CHEST TAB
-- ══════════════════════════════
local ChestTab = Window:CreateTab("Auto Chest", 4483345998)
ChestTab:CreateSection("Chest Farmer")
local autoChestRunning = false
local chestDelay = 0.5
local function teleportToChest(chest)
    local character = player.Character
    if not character then return end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local root = chest:FindFirstChildWhichIsA("BasePart") or (chest:IsA("BasePart") and chest)
    if root then hrp.CFrame = root.CFrame * CFrame.new(0, 0, 2) end
end
local function autoChestLoop()
    while autoChestRunning do
        local chestsFolder = workspace:FindFirstChild("Chests")
        if chestsFolder then
            for _, chest in ipairs(chestsFolder:GetChildren()) do
                if not autoChestRunning then break end
                pcall(teleportToChest, chest)
                task.wait(chestDelay)
            end
        end
        task.wait(1)
    end
end
ChestTab:CreateToggle({
    Name = "Auto Chest", CurrentValue=false, Flag="AutoChest",
    Description = "วิ่งเก็บ chest ทุกตัวใน workspace.Chests",
    Callback = function(v)
        autoChestRunning = v
        if v then task.spawn(autoChestLoop) end
        Rayfield:Notify({ Title="Chest", Content=v and "เริ่มแล้ว!" or "หยุดแล้ว", Duration=2 })
    end,
})
ChestTab:CreateSlider({
    Name = "Delay ระหว่าง Chest", Range={0.1,3}, Increment=0.1,
    Suffix="s", CurrentValue=0.5, Flag="ChestDelay",
    Callback = function(v) chestDelay = v end,
})
ChestTab:CreateButton({
    Name = "เก็บ Chest 1 รอบ (Manual)", Description="วิ่งเก็บทุก chest 1 รอบทันที",
    Callback = function()
        local count = 0
        local chestsFolder = workspace:FindFirstChild("Chests")
        if chestsFolder then
            for _, chest in ipairs(chestsFolder:GetChildren()) do
                local s = pcall(teleportToChest, chest)
                if s then count += 1 end
                task.wait(chestDelay)
            end
        end
        Rayfield:Notify({ Title="Chest", Content="เก็บครบ "..count.." chest!", Duration=3 })
    end,
})

-- ══════════════════════════════
-- AUTO FRUIT TAB
-- ══════════════════════════════
local FruitTab = Window:CreateTab("Auto Fruit", 4483345998)
FruitTab:CreateSection("Fruit Farmer")
local autoFruitRunning = false
local fruitDelay = 0.5
local function getFruitList()
    local fruits = workspace:FindFirstChild("MapFolder")
        and workspace.MapFolder:FindFirstChild("Fruits")
    if not fruits then return {} end
    local children = fruits:GetChildren()
    local list = {}
    local namedFruits = { "Apple", "Banana", "Cantaloupe", "Golden Apple", "Green Apple", "Melon", "Prickly Pear" }
    for _, name in ipairs(namedFruits) do
        local f = fruits:FindFirstChild(name)
        if f then table.insert(list, f) end
    end
    for _, idx in ipairs({6,7,8,10,13}) do
        if children[idx] then
            local already = false
            for _, e in ipairs(list) do if e == children[idx] then already = true break end end
            if not already then table.insert(list, children[idx]) end
        end
    end
    return list
end
local function collectFruit(fruit)
    local character = player.Character
    if not character then return end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local root
    if fruit:IsA("BasePart") then root = fruit
    elseif fruit:IsA("Model") then root = fruit.PrimaryPart or fruit:FindFirstChildWhichIsA("BasePart")
    else root = fruit:FindFirstChildWhichIsA("BasePart") end
    if not root then return end
    root.CFrame = hrp.CFrame * CFrame.new(0, 0, -2)
    task.wait(0.05)
    local prompt = fruit:FindFirstChildOfClass("ProximityPrompt") or fruit:FindFirstChildWhichIsA("ProximityPrompt")
    if prompt then pcall(fireproximityprompt, prompt) end
end
local function autoFruitLoop()
    while autoFruitRunning do
        local fruitList = getFruitList()
        if #fruitList == 0 then
            Rayfield:Notify({ Title="Fruit", Content="ไม่พบ Fruit!", Duration=3 })
            task.wait(5)
        else
            for _, fruit in ipairs(fruitList) do
                if not autoFruitRunning then break end
                pcall(collectFruit, fruit) task.wait(fruitDelay)
            end
        end
        task.wait(2)
    end
end
FruitTab:CreateToggle({ Name="Auto Collect Fruit", CurrentValue=false, Flag="AutoFruit", Description="ดึง Fruit เข้า Character อัตโนมัติ",
    Callback=function(v) autoFruitRunning=v if v then task.spawn(autoFruitLoop) end Rayfield:Notify({Title="Fruit",Content=v and "เริ่ม!" or "หยุด",Duration=2}) end })
FruitTab:CreateSlider({ Name="Delay ระหว่าง Fruit", Range={0.1,3}, Increment=0.1, Suffix="s", CurrentValue=0.5, Flag="FruitDelay", Callback=function(v) fruitDelay=v end })
FruitTab:CreateButton({ Name="เก็บ Fruit 1 รอบ (Manual)", Description="ดึง Fruit ทุกตัว 1 รอบทันที",
    Callback=function() local fl=getFruitList() local c=0 for _,f in ipairs(fl) do local s=pcall(collectFruit,f) if s then c+=1 end task.wait(fruitDelay) end Rayfield:Notify({Title="Fruit",Content="เก็บ "..c.."/"..#fl,Duration=3}) end })

-- ══════════════════════════════
-- AUTO JUICE TAB
-- ══════════════════════════════
local JuiceTab = Window:CreateTab("Auto Juice", 4483345998)
JuiceTab:CreateSection("Juicing Bowl")
local autoJuiceRunning = false
local juiceDelay = 0.3
local function getJuiceParts()
    local path = workspace:FindFirstChild("MapFolder")
        and workspace.MapFolder:FindFirstChild("Island8")
        and workspace.MapFolder.Island8:FindFirstChild("Kitchen")
    if not path then return nil end
    local station = path:GetChildren()[2]
    if not station then return nil end
    local bowl = station:FindFirstChild("JuicingBowl")
    if not bowl then return nil end
    return { mixer=bowl:FindFirstChild("JuicingBowlMixer"), bowlPart=bowl:FindFirstChild("Bowl"), mixer1=bowl:FindFirstChild("Mixer1"), mixer2=bowl:FindFirstChild("Mixer2") }
end
local function tpToPart(part)
    local char=player.Character if not char then return end
    local hrp=char:FindFirstChild("HumanoidRootPart") if not hrp or not part then return end
    local root if part:IsA("BasePart") then root=part else root=part.PrimaryPart or part:FindFirstChildWhichIsA("BasePart") end
    if root then hrp.CFrame=root.CFrame*CFrame.new(0,0,2) end
end
local function firePrompt(part)
    if not part then return end
    local prompt=part:FindFirstChildOfClass("ProximityPrompt")
    if not prompt then for _,c in ipairs(part:GetDescendants()) do if c:IsA("ProximityPrompt") then prompt=c break end end end
    if prompt then pcall(fireproximityprompt, prompt) end
end
local function doJuiceCycle()
    local p=getJuiceParts() if not p then Rayfield:Notify({Title="Juice",Content="ไม่พบ JuicingBowl!",Duration=3}) return false end
    for _, part in ipairs({p.bowlPart, p.mixer1, p.mixer2, p.mixer}) do
        tpToPart(part) task.wait(juiceDelay) firePrompt(part) task.wait(juiceDelay)
    end
    return true
end
local function autoJuiceLoop()
    while autoJuiceRunning do
        local ok2,err=pcall(doJuiceCycle) if not ok2 then Rayfield:Notify({Title="Juice Error",Content=tostring(err),Duration=3}) end
        task.wait(1)
    end
end
JuiceTab:CreateToggle({ Name="Auto Juice", CurrentValue=false, Flag="AutoJuice", Description="Auto วน Juicing Bowl ทุก step",
    Callback=function(v) autoJuiceRunning=v if v then task.spawn(autoJuiceLoop) end Rayfield:Notify({Title="Juice",Content=v and "เริ่ม!" or "หยุด",Duration=2}) end })
JuiceTab:CreateSlider({ Name="Delay ระหว่าง Step", Range={0.1,2}, Increment=0.1, Suffix="s", CurrentValue=0.3, Flag="JuiceDelay", Callback=function(v) juiceDelay=v end })
JuiceTab:CreateButton({ Name="Juice 1 รอบ (Manual)", Description="ทำ Juicing 1 รอบทันที",
    Callback=function() task.spawn(function() local ok2,err=pcall(doJuiceCycle) Rayfield:Notify({Title="Juice",Content=ok2 and "เสร็จ!" or tostring(err),Duration=3}) end) end })

-- ══════════════════════════════
-- AUTO USE TOOL TAB
-- ══════════════════════════════
local ToolTab = Window:CreateTab("Auto Use Tool", 4483345998)
ToolTab:CreateSection("Auto Drink Juice")
local autoToolRunning = false
local toolDelay = 0.5
local function getJuiceTools()
    local bp=player:FindFirstChild("Backpack") local char=player.Character local list={}
    local function chk(item) if item and item:IsA("Tool") and item.Name:find("Juice") then table.insert(list,item) end end
    if bp then for _,i in ipairs(bp:GetChildren()) do chk(i) end end
    if char then for _,i in ipairs(char:GetChildren()) do chk(i) end end
    return list
end
local function autoToolLoop()
    while autoToolRunning do
        local juices=getJuiceTools()
        if #juices==0 then task.wait(2)
        else
            for _,tool in ipairs(juices) do
                if not autoToolRunning then break end
                local char=player.Character local hum=char and char:FindFirstChildOfClass("Humanoid")
                if not hum then task.wait(0.2) continue end
                pcall(function() hum:EquipTool(tool) end) task.wait(0.2)
                local eq=char:FindFirstChild(tool.Name)
                if eq then pcall(function() eq:Activate() end) end
                task.wait(toolDelay)
            end
        end
    end
end
ToolTab:CreateToggle({ Name="Auto Drink Juice", CurrentValue=false, Flag="AutoUseTool", Description="Equip และกินทุก Juice ใน Backpack วนซ้ำ",
    Callback=function(v) autoToolRunning=v if v then task.spawn(autoToolLoop) end Rayfield:Notify({Title="Juice",Content=v and "เริ่ม!" or "หยุด",Duration=2}) end })
ToolTab:CreateSlider({ Name="Delay ระหว่าง Juice", Range={0.1,5}, Increment=0.1, Suffix="s", CurrentValue=0.5, Flag="ToolDelay", Callback=function(v) toolDelay=v end })

-- ══════════════════════════════
-- BUY DRINK TAB
-- ══════════════════════════════
local DrinkTab = Window:CreateTab("Buy Drink", 4483345998)
DrinkTab:CreateSection("Better Drink Merchant")
local drinkAmount = 1
local drinkList = { {name="Smoothie+",label="Smoothie+ [Defense]"}, {name="Cider+",label="Cider+ [Melee]"}, {name="Lemonade+",label="Lemonade+ [Sniper]"}, {name="Juice+",label="Juice+ [Sword]"} }
local function getMerchant() local m=player:FindFirstChild("CurrentMerchant") return m and m.Value end
local function buyDrink(drinkName, amount)
    local merchant=getMerchant()
    if not merchant then
        local npc=workspace:FindFirstChild("Ignore") and workspace.Ignore:FindFirstChild("NPCs") and workspace.Ignore.NPCs:FindFirstChild("Shop") and workspace.Ignore.NPCs.Shop:FindFirstChild("Better Drink Merchant")
        if npc then
            local char=player.Character local hrp=char and char:FindFirstChild("HumanoidRootPart")
            local root=npc:FindFirstChild("HumanoidRootPart") or npc.PrimaryPart or npc:FindFirstChildWhichIsA("BasePart")
            if hrp and root then hrp.CFrame=root.CFrame*CFrame.new(0,0,3) task.wait(0.5) merchant=getMerchant() end
        end
    end
    if not merchant then Rayfield:Notify({Title="Error",Content="ไปคุย Better Drink Merchant ก่อน!",Duration=3}) return end
    fire("ShopBuy", {drinkName, merchant, amount})
    Rayfield:Notify({Title="Buy Drink", Content="ซื้อ "..drinkName.." x"..amount, Duration=2})
end
DrinkTab:CreateSlider({ Name="จำนวนที่จะซื้อ", Range={1,999}, Increment=1, Suffix="x", CurrentValue=1, Flag="DrinkAmount", Callback=function(v) drinkAmount=v end })
for _, drink in ipairs(drinkList) do
    DrinkTab:CreateButton({ Name="ซื้อ "..drink.label, Description="ซื้อตามจำนวนที่ตั้ง", Callback=function() buyDrink(drink.name, drinkAmount) end })
end
DrinkTab:CreateButton({ Name="ซื้อทุกตัว (All)", Description="ซื้อพร้อมกันทุกตัว", Callback=function() for _,d in ipairs(drinkList) do buyDrink(d.name,drinkAmount) task.wait(0.3) end end })

-- ══════════════════════════════
-- AUTO FARM TAB (TWEEN AROUND MOB)
-- ══════════════════════════════
local FarmTab = Window:CreateTab("Auto Farm", 4483345998)
FarmTab:CreateSection("Monster Farm")
local autoFarmRunning  = false
local farmDelay        = 0.1
local selectedMonsters = {}
local orbitDistance    = 5
local farmDamage       = 50
local selectedSkills   = {}
local selectedTools    = {}

local monsterList = {
    "Lv1 Crab","Lv3 Crab","Lv4 Crab","Lv5 Crab","Lv300 King Crab",
    "Lv4 Boar","Lv11 Boar","Lv12 Boar","Lv14 Boar","Lv15 Boar","Lv16 Boar",
    "Lv9 Bandit Traitor","Lv14 Bandit","Lv15 Bandit",
    "Lv12 Thug","Lv15 Thug","Lv17 Thug","Lv22 Thug","Lv23 Thug","Lv24 Thug","Lv30 Thug","Lv40 Thug",
    "Lv2 Angry Bob","Lv22 Angry Bobby","Lv24 Angry Bobbi","Lv29 Angry Bobber","Lv35 Angry Bobb",
    "Lv4 Angry Freddy","Lv24 Fred","Lv28 Fredde","Lv28 Freyd","Lv28 Friedrich","Lv29 Frued","Lv32 Fredric","Lv34 Freddi",
    "Lv20 Gunslinger","Lv24 Gunslinger","Lv36 Gunslinger","Lv42 Gunslinger","Lv50 Gunslinger",
    "Lv20 Thief","Lv32 Thief",
    "Lv186 Cave Demon","Lv188 Cave Demon","Lv198 Cave Demon","Lv219 Cave Demon",
    "Lv40 Cave Demon [Weakened]",
    "Lv360 Bruno","Lv440 Buster","Lv500 Bucky",
    "Lv2000 Crocodile","Lv2000 Vokun","Lv8000 Gunner Captain",
    "ThNerdZ",
}

local skillList = { "x","c","v","b","n","f","g","h","j","k","l" }

local function getBackpackTools()
    local list = {}
    local bp = player:FindFirstChild("Backpack")
    local char = player.Character
    if bp then
        for _, t in ipairs(bp:GetChildren()) do
            if t:IsA("Tool") then table.insert(list, t.Name) end
        end
    end
    if char then
        for _, t in ipairs(char:GetChildren()) do
            if t:IsA("Tool") then table.insert(list, t.Name) end
        end
    end
    return list
end

local function findAliveMonsters(nameList)
    local alive = workspace:FindFirstChild("Alive")
    if not alive then return {} end
    local nameSet = {}
    for _, n in ipairs(nameList) do nameSet[n] = true end
    local found = {}
    for _, model in ipairs(alive:GetChildren()) do
        if nameSet[model.Name] then
            local hum = model:FindFirstChildOfClass("Humanoid")
            local hrp = model:FindFirstChild("HumanoidRootPart")
            if hum and hrp and hum.Health > 0 then
                table.insert(found, { model=model, hum=hum, hrp=hrp })
            end
        end
    end
    return found
end

-- Tween char วนรอบ mob (เปลี่ยนมุมทีละนิด)
local orbitAngle = 0
local function orbitAroundMob(monHRP)
    local char = player.Character
    if not char then return end
    local myHRP = char:FindFirstChild("HumanoidRootPart")
    if not myHRP then return end
    orbitAngle = (orbitAngle + 45) % 360
    local rad = math.rad(orbitAngle)
    local offset = Vector3.new(math.cos(rad) * orbitDistance, 0, math.sin(rad) * orbitDistance)
    local targetCF = CFrame.new(monHRP.Position + offset, monHRP.Position)
    -- tween ด้วย TweenService
    local TweenService = game:GetService("TweenService")
    local info = TweenInfo.new(0.15, Enum.EasingStyle.Linear)
    local tween = TweenService:Create(myHRP, info, { CFrame = targetCF })
    tween:Play()
    tween.Completed:Wait()
end

-- กดสกิล keyboard
local function fireSkills()
    local VirtualInput = game:GetService("VirtualInputManager")
    local keyMap = {
        x = Enum.KeyCode.X, c = Enum.KeyCode.C, v = Enum.KeyCode.V,
        b = Enum.KeyCode.B, n = Enum.KeyCode.N, f = Enum.KeyCode.F,
        g = Enum.KeyCode.G, h = Enum.KeyCode.H, j = Enum.KeyCode.J,
        k = Enum.KeyCode.K, l = Enum.KeyCode.L,
    }
    for _, sk in ipairs(selectedSkills) do
        local kc = keyMap[sk]
        if kc then
            pcall(function()
                VirtualInput:SendKeyEvent(true,  kc, false, game)
                task.wait(0.05)
                VirtualInput:SendKeyEvent(false, kc, false, game)
            end)
            task.wait(0.05)
        end
    end
end

-- Equip tool จาก backpack แล้ว activate
local function fireTools()
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    for _, toolName in ipairs(selectedTools) do
        local bp = player:FindFirstChild("Backpack")
        local tool = (bp and bp:FindFirstChild(toolName))
            or char:FindFirstChild(toolName)
        if tool then
            pcall(function() hum:EquipTool(tool) end)
            task.wait(0.1)
            local eq = char:FindFirstChild(toolName)
            if eq then pcall(function() eq:Activate() end) end
            task.wait(0.05)
        end
    end
end

local function attackMob(entry)
    local model = entry.model
    local hum   = entry.hum
    if not (model and hum) or hum.Health <= 0 then return end
    if v1 then
        pcall(function() v1["\t"]("MobHit",    { model, farmDamage }) end)
        pcall(function() v1["\t"]("DamageNPC", { model, farmDamage }) end)
    end
    pcall(function() hum:TakeDamage(farmDamage) end)
    fireSkills()
    fireTools()
end

local function autoFarmLoop()
    while autoFarmRunning do
        if #selectedMonsters == 0 then
            Rayfield:Notify({ Title="Farm", Content="ยังไม่ได้เลือก Monster!", Duration=2 })
            task.wait(3)
            continue
        end

        local targets = findAliveMonsters(selectedMonsters)
        if #targets == 0 then task.wait(1) continue end

        for _, entry in ipairs(targets) do
            if not autoFarmRunning then break end
            local hum = entry.hum
            local hrp = entry.hrp

            if not hum or not hrp or hum.Health <= 0 then continue end

            -- teleport ไปใกล้ mob ก่อน
            local char = player.Character
            local myHRP = char and char:FindFirstChild("HumanoidRootPart")
            if myHRP then
                myHRP.CFrame = hrp.CFrame * CFrame.new(0, 0, orbitDistance)
            end
            task.wait(0.05)

            -- วน tween + ตี จนกว่า mob จะตาย
            while autoFarmRunning do
                if not hum or not hum.Parent or hum.Health <= 0 then break end
                pcall(orbitAroundMob, hrp)
                pcall(attackMob, entry)
                task.wait(farmDelay)
            end
        end
    end
end

-- ── UI ──
FarmTab:CreateDropdown({
    Name = "เลือก Monster", Options = monsterList, CurrentOption = {},
    Flag = "SelectedMonsters", MultipleOptions = true,
    Description = "เลือก Monster ที่ต้องการ Farm",
    Callback = function(v)
        selectedMonsters = v
        Rayfield:Notify({ Title="Farm", Content="เลือก "..#v.." monster", Duration=2 })
    end,
})
FarmTab:CreateDropdown({
    Name = "เลือก Skill Keys", Options = skillList, CurrentOption = {},
    Flag = "SelectedSkills", MultipleOptions = true,
    Description = "เลือก key สกิลที่จะกดระหว่าง farm (x,c,v,b,n,f,g,h,j,k,l)",
    Callback = function(v)
        selectedSkills = v
        Rayfield:Notify({ Title="Skills", Content="เลือก "..#v.." skill", Duration=2 })
    end,
})
FarmTab:CreateButton({
    Name = "Refresh Tool List", Description = "โหลด Tool ใหม่จาก Backpack",
    Callback = function()
        local tools = getBackpackTools()
        Rayfield:Notify({ Title="Tools", Content="พบ "..#tools.." tool: "..table.concat(tools,", "), Duration=4 })
    end,
})
FarmTab:CreateInput({
    Name = "เลือก Tool (พิมพ์ชื่อ คั่นด้วย ,)", PlaceholderText = "เช่น Sword,Pistol",
    RemoveTextAfterFocusLost = false, Flag = "SelectedTools",
    Callback = function(v)
        selectedTools = {}
        for part in v:gmatch("[^,]+") do
            table.insert(selectedTools, part:match("^%s*(.-)%s*$"))
        end
        Rayfield:Notify({ Title="Tools", Content="เลือก "..#selectedTools.." tool", Duration=2 })
    end,
})
FarmTab:CreateToggle({
    Name = "Auto Farm (Tween + Skill + Tool)", CurrentValue = false, Flag = "AutoFarm",
    Description = "วาปไปหา mob, tween วนรอบ, กดสกิล และใช้ tool",
    Callback = function(v)
        autoFarmRunning = v
        if v then task.spawn(autoFarmLoop) end
        Rayfield:Notify({ Title="Farm", Content=v and "เริ่ม Farm!" or "หยุดแล้ว", Duration=2 })
    end,
})
FarmTab:CreateSlider({
    Name = "ระยะวนรอบ Mob (Studs)", Range={1,20}, Increment=1,
    Suffix=" studs", CurrentValue=5, Flag="OrbitDistance",
    Callback = function(v) orbitDistance = v end,
})
FarmTab:CreateSlider({
    Name = "Damage per Hit", Range={1,9999}, Increment=1,
    Suffix=" dmg", CurrentValue=50, Flag="FarmDamage",
    Callback = function(v) farmDamage = v end,
})
FarmTab:CreateSlider({
    Name = "Delay ระหว่าง Hit", Range={0.05,3}, Increment=0.05,
    Suffix="s", CurrentValue=0.1, Flag="FarmDelay",
    Callback = function(v) farmDelay = v end,
})
FarmTab:CreateButton({
    Name = "Debug: แสดง Monster ใน Alive", Description = "แสดงชื่อ + HP ใน console (F9)",
    Callback = function()
        local alive = workspace:FindFirstChild("Alive")
        if not alive then
            Rayfield:Notify({ Title="Debug", Content="ไม่พบ workspace.Alive!", Duration=3 })
            return
        end
        print("=== workspace.Alive ("..#alive:GetChildren().." ตัว) ===")
        for i, v in ipairs(alive:GetChildren()) do
            local h = v:FindFirstChildOfClass("Humanoid")
            print(i, v.Name, h and ("HP:"..h.Health) or "(no Humanoid)")
        end
        Rayfield:Notify({ Title="Debug", Content="ดูผลใน F9", Duration=3 })
    end,
})

-- ══════════════════════════════
-- MISC TAB
-- ══════════════════════════════
local MiscTab = Window:CreateTab("Misc", 4483345998)
MiscTab:CreateSection("Settings")
MiscTab:CreateToggle({
    Name="Anti AFK", CurrentValue=false, Flag="AntiAFK",
    Callback=function(v)
        if v then
            local vp=game:GetService("VirtualUser")
            Players.LocalPlayer.Idled:Connect(function()
                vp:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
                task.wait(1)
                vp:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            end)
        end
    end,
})

Rayfield:LoadConfiguration()


-- ══════════════════════════════
-- AIMBOT TAB
-- ══════════════════════════════
local LockTab = Window:CreateTab("AIMBOT", 4483345998)

LockTab:CreateToggle({
    Name = "PVP",
    CurrentValue = false,
    Flag = "LOCK",

    Callback = function(Value)

        getgenv().LockEnabled = Value

        task.spawn(function()
            while getgenv().LockEnabled do

                local ClosestPlayer
                local ClosestDistance = math.huge

                local LocalCharacter = player.Character
                local LocalHRP = LocalCharacter and LocalCharacter:FindFirstChild("HumanoidRootPart")

                if LocalHRP then
                    for _, Plr in ipairs(Players:GetPlayers()) do

                        if Plr ~= player and Plr.Character then

                            local HRP = Plr.Character:FindFirstChild("HumanoidRootPart")
                            local Humanoid = Plr.Character:FindFirstChild("Humanoid")

                            if HRP and Humanoid and Humanoid.Health > 0 then

                                local Distance = (LocalHRP.Position - HRP.Position).Magnitude

                                if Distance < ClosestDistance then
                                    ClosestDistance = Distance
                                    ClosestPlayer = Plr
                                end
                            end
                        end
                    end
                end

                if ClosestPlayer and ClosestPlayer.Character then
                    local HRP = ClosestPlayer.Character:FindFirstChild("HumanoidRootPart")

                    if HRP then
                        workspace.CurrentCamera.CFrame =
                            CFrame.new(
                                workspace.CurrentCamera.CFrame.Position,
                                HRP.Position
                            )
                    end
                end

                task.wait()
            end
        end)
    end
})
